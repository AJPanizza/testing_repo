# This is a Databricks asset bundle definition for n8n_rag.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: n8n_rag
  uuid: a957360b-e03c-4efd-bded-6aaaaa58f147

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-56f60291-144c.cloud.databricks.com
    variables:
      catalog: agustin_training_catalog
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-56f60291-144c.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/agustin.panizza@sunnydata.ai to make sure we only have a single copy.
      root_path: /Workspace/Users/agustin.panizza@sunnydata.ai/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: agustin_training_catalog
      schema: prod
    permissions:
      - user_name: agustin.panizza@sunnydata.ai
        level: CAN_MANAGE
