resources:
  schemas:
    main:
      name: ${var.schema}
      catalog_name: ${var.catalog}

  volumes:
    libraries:
      name: ${var.volume}
      catalog_name: ${var.catalog}
      schema_name: ${resources.schemas.main.name}
      volume_type: MANAGED
